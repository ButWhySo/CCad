#include "eda_importers.hpp"

namespace ccad {

bool EdaImporters::importAllegro(const std::string& filepath) { return !filepath.empty(); }
bool EdaImporters::importAltium(const std::string& filepath) { return !filepath.empty(); }
bool EdaImporters::importCadstar(const std::string& filepath) { return !filepath.empty(); }
bool EdaImporters::importEagle(const std::string& filepath) { return !filepath.empty(); }
bool EdaImporters::importEasyEda(const std::string& filepath) { return !filepath.empty(); }
bool EdaImporters::importEasyEdaPro(const std::string& filepath) { return !filepath.empty(); }
bool EdaImporters::importFabmaster(const std::string& filepath) { return !filepath.empty(); }
bool EdaImporters::importGeda(const std::string& filepath) { return !filepath.empty(); }
bool EdaImporters::importPads(const std::string& filepath) { return !filepath.empty(); }
bool EdaImporters::importPcad(const std::string& filepath) { return !filepath.empty(); }
bool EdaImporters::importSprintLayout(const std::string& filepath) { return !filepath.empty(); }

} // namespace ccad
