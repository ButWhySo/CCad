#include "gerber_collectors.hpp"

namespace ccad {

std::vector<GerberDrawItem*> GerberCollectors::collectAtPoint(const Point& pt, const std::vector<GerberDrawItem*>& items) {
    std::vector<GerberDrawItem*> result;
    // Stub implementation to hit test items at the given point
    return result;
}

} // namespace ccad
