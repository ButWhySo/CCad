#include "agent_policy.hpp"

namespace ccad_cli {

// Policy behavior now lives in ccad_core/agent_policy.cpp so the CLI, GUI, and
// future runner share one classifier. This file remains as a compatibility
// source path for agents and build scripts that know the old module location.

}  // namespace ccad_cli
