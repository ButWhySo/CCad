Stabilize ccad_core build and restore visual validation

Why:
Refactoring core dependencies (e.g., merging board into model.hpp) and enabling strict -Werror caused cascading build failures in plugins and GUI bridges due to missing includes and unused parameters.

Changed:
- Replaced #include "board.hpp" with #include "model.hpp" across 20+ core files.
- Added explicit (void) casts to suppress unused parameter warnings in interactive tools, DRC providers, and exporters.
- Updated ccad_gui components to align with model.hpp structs.

Behavior:
- Project now cleanly builds under -Werror with Qt 6 MinGW.
- ccad_gui visual harness renders correctly with no missing headers.

Verification:
- Ran full cmake --build build-qt --target ccad_gui.
- Executed scripts/run_sprint_demo.ps1 with intercepted stdout/stderr.

Demo:
- Ingested screenshot showing correct footprint and layer rendering.
